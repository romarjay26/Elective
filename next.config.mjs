/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Elective",
  assetPrefix: "/Elective/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;